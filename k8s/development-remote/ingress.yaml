apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-toto-MonApppp-backend-ingress
  namespace: dev-toto-MonApppp-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - dev.testdomain.api.flapi.org
      secretName: dev-toto-MonApppp-backend-tls
  rules:
    - host: dev.testdomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: dev-toto-MonApppp-backend-service
                port:
                  number: 3333
