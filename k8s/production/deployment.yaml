apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-toto-MonApppp-backend-deployment
  namespace: prod-toto-MonApppp-backend
  labels:
    app: prod-toto-MonApppp-backend
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: prod-toto-MonApppp-backend
  template:
    metadata:
      labels:
        app: prod-toto-MonApppp-backend
    spec:
      containers:
        - name: prod-toto-MonApppp-backend
          image: corentin35/toto-MonApppp-backend:${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
