apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: staging-toto-MonApppp-backend-ingress
  namespace: staging-toto-MonApppp-backend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - staging.testdomain.api.flapi.org
      secretName: staging-toto-MonApppp-backend-tls
  rules:
    - host: staging.testdomain.api.flapi.org
      http:
        paths:
          - pathType: Prefix
            path: '/'
            backend:
              service:
                name: staging-toto-MonApppp-backend-service
                port:
                  number: 3333
