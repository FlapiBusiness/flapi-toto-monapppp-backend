apiVersion: apps/v1
kind: Deployment
metadata:
  name: staging-toto-MonApppp-backend-deployment
  namespace: staging-toto-MonApppp-backend
  labels:
    app: staging-toto-MonApppp-backend
spec:
  selector:
    matchLabels:
      app: staging-toto-MonApppp-backend
  template:
    metadata:
      labels:
        app: staging-toto-MonApppp-backend
    spec:
      containers:
        - name: staging-toto-MonApppp-backend
          image: corentin35/toto-MonApppp-backend:staging-${TAG_VERSION}
          imagePullPolicy: Always
          ports:
            - containerPort: 3333
          env:
            - name: PORT
              value: '3333'
          resources:
            requests:
              cpu: '300m'
              memory: '400Mi'
            limits:
              cpu: '600m'
              memory: '800Mi'
      imagePullSecrets:
        - name: regcred
